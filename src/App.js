import React from 'react';
import { FloatingMenu } from './FloatingMenu';

const App = () => (
  <div>
    <h2>Hello, World</h2>
    <FloatingMenu>
      Children
    </FloatingMenu>
  </div>
);

export default App;
